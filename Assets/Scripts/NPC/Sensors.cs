using System.Collections;
using System.Collections.Generic;
using System.Runtime;
using Unity.VisualScripting.YamlDotNet.Core.Tokens;
using UnityEngine;

//public class RayHit : ISensors
//{ 

//    public float Value(Vector3 origin, Vector3 direction, string tag, float maxDistance, float hitDivider)
//{
//    float dist = 0;
//    Ray r = new(origin, direction);
//    RaycastHit hit;


//    if (Physics.Raycast(r, out hit, maxDistance) && hit.transform.CompareTag(tag))
//    {
//        Debug.DrawLine(r.origin, hit.point, Color.white);
//        dist = hit.distance / hitDivider;
//    }
//    return dist;
//}
//}
